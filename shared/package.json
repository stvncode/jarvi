{
  "name": "@jarvi/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for Jarvi stats",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "typescript": "^5.3.0"
  }
} 